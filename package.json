{
  "name": "ignis-assignment",
  "version": "1.0.0",
  "private": true,
  "description": "Express OAuth authorize endpoint boilerplate",
  "main": "src/server.js",
  "scripts": {
    "dev": "node --watch src/server.js",
    "start": "node src/server.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "express": "^5.2.1"
  }
}
